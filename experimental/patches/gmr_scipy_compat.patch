diff --git a/general_motion_retargeting/motion_retarget.py b/general_motion_retargeting/motion_retarget.py
index b68bfdc..c737567 100644
--- a/general_motion_retargeting/motion_retarget.py
+++ b/general_motion_retargeting/motion_retarget.py
@@ -7,6 +7,16 @@ from scipy.spatial.transform import Rotation as R
 from .params import ROBOT_XML_DICT, IK_CONFIG_DICT
 from rich import print
 
+
+def _rotation_from_quat_wxyz(quat):
+    quat = np.asarray(quat, dtype=np.float64).reshape(4)
+    return R.from_quat(np.array([quat[1], quat[2], quat[3], quat[0]], dtype=np.float64))
+
+
+def _rotation_as_quat_wxyz(rotation):
+    quat_xyzw = rotation.as_quat()
+    return np.array([quat_xyzw[3], quat_xyzw[0], quat_xyzw[1], quat_xyzw[2]], dtype=np.float64)
+
 class GeneralMotionRetargeting:
     """General Motion Retargeting (GMR).
     """
@@ -122,9 +132,7 @@ class GeneralMotionRetargeting:
                 )
                 self.human_body_to_task1[body_name] = task
                 self.pos_offsets1[body_name] = np.array(pos_offset) - self.ground
-                self.rot_offsets1[body_name] = R.from_quat(
-                    rot_offset, scalar_first=True
-                )
+                self.rot_offsets1[body_name] = _rotation_from_quat_wxyz(rot_offset)
                 self.tasks1.append(task)
                 self.task_errors1[task] = []
         
@@ -140,9 +148,7 @@ class GeneralMotionRetargeting:
                 )
                 self.human_body_to_task2[body_name] = task
                 self.pos_offsets2[body_name] = np.array(pos_offset) - self.ground
-                self.rot_offsets2[body_name] = R.from_quat(
-                    rot_offset, scalar_first=True
-                )
+                self.rot_offsets2[body_name] = _rotation_from_quat_wxyz(rot_offset)
                 self.tasks2.append(task)
                 self.task_errors2[task] = []
 
@@ -272,12 +278,12 @@ class GeneralMotionRetargeting:
             pos, quat = human_data[body_name]
             offset_human_data[body_name] = [pos, quat]
             # apply rotation offset first
-            updated_quat = (R.from_quat(quat, scalar_first=True) * rot_offsets[body_name]).as_quat(scalar_first=True)
+            updated_quat = _rotation_as_quat_wxyz(_rotation_from_quat_wxyz(quat) * rot_offsets[body_name])
             offset_human_data[body_name][1] = updated_quat
             
             local_offset = pos_offsets[body_name]
             # compute the global position offset using the updated rotation
-            global_pos_offset = R.from_quat(updated_quat, scalar_first=True).apply(local_offset)
+            global_pos_offset = _rotation_from_quat_wxyz(updated_quat).apply(local_offset)
             
             offset_human_data[body_name][0] = pos + global_pos_offset
            
diff --git a/general_motion_retargeting/neck_retarget.py b/general_motion_retargeting/neck_retarget.py
index d06627a..bb38aac 100644
--- a/general_motion_retargeting/neck_retarget.py
+++ b/general_motion_retargeting/neck_retarget.py
@@ -1,6 +1,11 @@
 import numpy as np
 from scipy.spatial.transform import Rotation as R
 
+
+def _rotation_from_quat_wxyz(quat):
+    quat = np.asarray(quat, dtype=np.float64).reshape(4)
+    return R.from_quat(np.array([quat[1], quat[2], quat[3], quat[0]], dtype=np.float64))
+
 def get_human_neck_orientation(head_pose=None):
       """
       从XRoboToolkit SDK获取头戴设备姿态并计算neck的roll, pitch, yaw角度
@@ -30,8 +35,8 @@ def human_head_to_robot_neck(smplx_data=None):
     head_rotation = smplx_data['Head'][1] # wxyz
 
     # Convert to rotation objects
-    spine_rotation = R.from_quat(spine_rotation, scalar_first=True)
-    head_rotation = R.from_quat(head_rotation, scalar_first=True)
+    spine_rotation = _rotation_from_quat_wxyz(spine_rotation)
+    head_rotation = _rotation_from_quat_wxyz(head_rotation)
     
     # compute the rpy of the head in local frame (relative to spine)
     # Get the relative rotation: head relative to spine
